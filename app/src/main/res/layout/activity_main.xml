<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    tools:context=".MainActivity">


    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="결과"
        android:textSize="20sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/resultTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:layout_marginStart="4dp"
        android:textSize="16sp"
        tools:text="결과값" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:text="Firebase 토큰"
        android:textSize="20sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/firebaseTokenTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="Loading..."
        android:layout_marginStart="16dp"
        android:textIsSelectable="true"
        android:textSize="16sp" />

</LinearLayout>

    <!--
    textIsSelectable : 편집할 수 없는 텍스트의 내용을 선택할 수 있음을 나타냅니다.
    marginStart 와 marginLeft 차이 : 다른 나라의 언어중에서 오른쪽에서 시작하여 왼쪽으로 가는 방향을 사용하는 언어들(RtL언어)이
    존재하는데 Left로 설정할 경우 무조건 왼쪽을 띄우고 Start로 설정할 경우 오른쪽에서 시작하는 언어일 경우에도 margin을 적용시켜준다.


    -->